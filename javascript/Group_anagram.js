/**
 * @param {string[]} strs
 * @return {string[][]}
 */
var groupAnagrams = function(strs) {
    table_map = new Map();
    
    for (strings in strs){
        str_array = new Array[26].fill(0);
        for(i = 0; i < strs.length; i++){
            key = str_array[strs.charCodeat(i) - 'a'.charCodeAt(0)]++   
        }

        if(table_map.has(key)){
            t_key = 

        }
    }
    
};

// create table 
// keys will be tables that have specific letters
// values to key --> will be the strings whose letters corresponds to table

// loop through array of strings, 


// create an array of 26 spaces, fill with 0, 
// using unicode --> 

// have to compare strings version of the keys.